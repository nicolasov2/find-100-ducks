import type { Vector3Tuple } from 'three';
import type { SpawnPoint } from '@/game/types';

const POSITIONS: readonly Vector3Tuple[] = [
  // ── Table top (6) ──
  [-1.7, 0.95, -0.8], [0.7, 0.95, -0.8], [1.7, 0.95, 0.8],
  [0.0, 0.95, 0.0], [-1.0, 0.95, 0.5], [1.5, 0.95, 0.6],
  // ── Under table (hidden — need to crouch/look down) (3) ──
  [0, 0.06, 0], [-1.0, 0.06, -0.5], [1.2, 0.06, 0.3],
  // ── Chairs — on seat (4) ──
  [0, 0.58, -2], [0, 0.58, 2], [2.5, 0.58, 0], [-2.5, 0.58, 0],
  // ── Sofa cushions + behind sofa (5) ──
  [-5.5, 0.70, 7], [-3.0, 0.70, 7], [-4.0, 0.06, 7.8],
  [-6.4, 1.10, 7.5], [-1.6, 0.06, 7.3],
  // ── TV stand — behind TV, inside cabinet, on top (4) ──
  [-4, 0.06, 4.2], [-4.3, 0.30, 4.5], [-3.7, 0.30, 4.5], [-4, 1.25, 4.5],
  // ── Side tables — under/on (4) ──
  [-7, 0.55, 7], [-7, 0.06, 7.2], [-1, 0.55, 7.5], [8.5, 0.55, -8.5],
  // ── Bed under + pillows + behind headboard (5) ──
  [6.0, 0.65, -7.0], [5.4, 0.88, -8.0], [6.6, 0.88, -8.0],
  [6.0, 0.06, -5.3], [6.0, 0.06, -8.8],
  // ── Shelf west — between books (4) ──
  [-6.7, 0.55, -9.4], [-6.0, 1.55, -9.4], [-5.3, 2.55, -9.4], [-6.0, 3.10, -9.4],
  // ── Shelf east — between books (4) ──
  [4.7, 0.55, -9.4], [4.0, 1.55, -9.4], [3.3, 2.55, -9.4], [4.0, 3.10, -9.4],
  // ── Extra shelf west wall (3) ──
  [-9.5, 0.55, -4], [-9.5, 1.55, -4], [-9.5, 2.55, -4],
  // ── Behind/beside boxes (5) ──
  [-8.4, 0.06, 5.9], [8.9, 0.06, -5.1], [-2.4, 0.06, -8.1],
  [7.3, 0.06, 3.2], [-8.8, 0.06, -5.7],
  // ── Inside open boxes (3) ──
  [-8, 0.30, 5.5], [8.5, 0.30, -5.5], [7, 0.30, 3],
  // ── Book stacks on top (4) ──
  [1.6, 1.25, -0.6], [-1.7, 1.25, 0.6], [-7, 0.90, 7], [8.5, 0.90, -8.5],
  // ── Plants — hidden among foliage (5) ──
  [-9.3, 0.60, -9.0], [-8.8, 0.60, 8.9], [8.6, 0.60, -2.0],
  [3, 0.60, 9.0], [-4, 0.60, -6.0],
  // ── Cushions — next to (3) ──
  [2.0, 0.22, 5.0], [-3.0, 0.22, 4.0], [5.0, 0.22, 5.5],
  // ── Coat rack base — behind pole (2) ──
  [9.0, 0.06, -9.2], [-9.5, 0.06, 0.3],
  // ── Wall painting ledge — on top of frame (3) ──
  [-3, 2.65, -9.8], [7, 3.0, -9.8], [-9.8, 2.40, 5],
  // ── Lamp shade (2) ──
  [8, 2.55, 8], [-9, 2.55, 3],
  // ── Dark corners — flush against walls (6) ──
  [-9.8, 0.06, -9.8], [9.8, 0.06, -9.8], [9.8, 0.06, 9.8], [-9.8, 0.06, 9.8],
  [-9.8, 0.06, -2], [9.8, 0.06, 6],
  // ── High spots — on top of furniture (3) ──
  [-6, 3.20, -9.5], [4, 3.20, -9.5], [-9.5, 3.10, -4],
  // ── Scattered open floor (8) ──
  [-5, 0.06, -3], [5, 0.06, -3], [0, 0.06, -7],
  [-7, 0.06, -7], [7, 0.06, 5], [3, 0.06, 8],
  [-6, 0.06, 2], [0, 0.06, 5],
];

export const SPAWN_POOL_ROOM_1: readonly SpawnPoint[] = POSITIONS.map((p, i) => ({
  id: `r1-${String(i + 1).padStart(3, '0')}`,
  position: p,
  roomId: 'room-1',
}));
